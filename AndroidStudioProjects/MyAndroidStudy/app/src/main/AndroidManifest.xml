<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bluesweater.myandroidstudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BasicHandlerActivity" />
        <activity android:name=".BasicHandlerActivity2" />
        <activity android:name=".BasicHandlerActivity3" />
        <activity android:name=".ThreadLifeCycleActivity" />
        <activity android:name=".ThreadLifeCyclyeFragmentActivity"></activity>
        <activity android:name=".HandlerThreadActivity"></activity>
        <activity android:name=".ChainedNetworkActivity"></activity>
        <activity android:name=".AsyncTaskActivity"></activity>
    </application>

</manifest>